# -*- coding: utf-8 -*-

from sklearn.metrics import confusion_matrix

def computeConfusionMatrix(data, gt, confmat, logger, ram=str(128)):
    logger.message("confusion matrix function not available", console=True, warning=True)
    return
